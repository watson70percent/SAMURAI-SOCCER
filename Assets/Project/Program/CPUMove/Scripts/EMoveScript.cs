using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EMoveScript : MonoBehaviour
{
    public int[] field = { 80, 100, 80 };

    enum moveDirection
    {
        NE,
        E,
        SE,
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
